# ===================================================================
# PDF Pal Frontend Environment Configuration
# ===================================================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env (Mac/Linux) or copy .env.example .env (Windows)
# 2. Update URLs if your backend runs on a different port
# 3. For production, change to your actual domain
#
# ===================================================================

# Backend API URL (FastAPI server)
# Default: http://localhost:8000
# Change if backend runs on different port or domain
VITE_API_URL=http://localhost:8000

# Socket.IO WebSocket URL (usually same as API URL)
# Default: http://localhost:8000
# Must match backend server address for real-time chat
VITE_SOCKET_URL=http://localhost:8000

# ===================================================================
# PRODUCTION SETTINGS
# ===================================================================
# For production deployment, change to your actual domain:
# VITE_API_URL=https://api.yourapp.com
# VITE_SOCKET_URL=https://api.yourapp.com

# ===================================================================
# NOTES:
# - These URLs are embedded into frontend build at build time
# - After changing, restart dev server: npm run dev
# - For production build: npm run build
# - Vite only exposes variables prefixed with VITE_
# ===================================================================
