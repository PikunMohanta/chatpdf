# ===================================================================
# PDF Pal Backend Environment Configuration
# ===================================================================
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env (Mac/Linux) or copy .env.example .env (Windows)
# 2. Fill in REQUIRED values marked with ⚠️
# 3. Optional values can be left as-is for development
#
# For detailed setup guide, see README.md
# ===================================================================

# ===================================================================
# 🔴 REQUIRED FOR AI CHAT RESPONSES
# ===================================================================
# Get your free API key from: https://openrouter.ai/keys
# Without this, you'll only get mock responses!
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# ===================================================================
# 🟢 OPTIONAL - Development (defaults work without AWS)
# ===================================================================

# General Settings
DEBUG=True
SECRET_KEY=dev-secret-key-change-in-production-to-random-string
HOST=0.0.0.0
PORT=8000

# Frontend URL (for CORS) - change if frontend runs on different port
FRONTEND_URL=http://localhost:3000

# ===================================================================
# 🟡 OPTIONAL - Production/AWS Features (not needed for local dev)
# ===================================================================

# AWS S3 Storage (leave empty to use local file storage)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_DEFAULT_REGION=us-east-1
# S3_BUCKET_NAME=your-bucket-name

# AWS Cognito Authentication (leave empty to use dev token auth)
# COGNITO_USER_POOL_ID=your-cognito-user-pool-id
# COGNITO_CLIENT_ID=your-cognito-client-id
# COGNITO_REGION=us-east-1

# Database - PostgreSQL (leave empty to use JSON file storage)
# DATABASE_URL=postgresql://username:password@localhost:5432/chatpdf

# Redis Cache (leave empty to disable caching)
# REDIS_URL=redis://localhost:6379/0

# Celery Background Tasks (leave empty to disable)
# CELERY_BROKER_URL=redis://localhost:6379/1
# CELERY_RESULT_BACKEND=redis://localhost:6379/2

# ===================================================================
# 🔧 ADVANCED SETTINGS (rarely need to change)
# ===================================================================

# File Upload Limits
MAX_FILE_SIZE=50  # Maximum PDF size in MB
ALLOWED_FILE_TYPES=pdf

# Logging
LOG_LEVEL=INFO
# LOG_FILE=logs/app.log

# OpenAI API (alternative to OpenRouter - not currently used)
# OPENAI_API_KEY=your-openai-api-key

# ===================================================================
# NOTES:
# - Development mode works WITHOUT AWS, Database, or Redis
# - Local storage: backend/data/uploads/
# - Chat history: backend/data/chat_history/
# - Mock embeddings: backend/data/mock_embeddings/
# - Dev authentication: Use "dev-token" as Bearer token
# ===================================================================